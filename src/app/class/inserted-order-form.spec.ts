import { InsertedOrderForm } from './inserted-order-form';

describe('InsertedOrderForm', () => {
  it('should create an instance', () => {
    expect(new InsertedOrderForm()).toBeTruthy();
  });
});
